extern crate nalgebra as na;

// use regret_matching::RegretMatchingAgent;

fn main() {
    // let agent : RegretMatchingAgent;
    let v = na::Vector3::new(1.,2.,3.);
    println!("Hello! {}", v+3.*v);
}
